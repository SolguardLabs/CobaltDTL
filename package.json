{
  "name": "cobaltdtl",
  "version": "1.0.0",
  "private": true,
  "description": "Synthetic vault simulator with internal credits and deferred redemption batches.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs --warnings",
    "format:check": "prettier --check .",
    "check": "tsc --noEmit",
    "test:cpp": "node scripts/test-cpp.mjs",
    "test:node": "node --test --experimental-strip-types \"tests/node/*.test.ts\"",
    "test": "npm run test:cpp && npm run build && npm run test:node",
    "verify:repo": "node scripts/verify-repository.mjs",
    "ci": "npm run format:check && npm run check && npm run test && npm run verify:repo"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "prettier": "^3.6.2",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=24"
  }
}
